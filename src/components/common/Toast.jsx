import React from 'react';

/**
 * Toast Component
 * This is a placeholder component since App.jsx imports it
 * The actual toast notifications are handled by NotificationToastContext and ToastNotification
 * This component doesn't render anything - it's just to prevent import errors
 */
const Toast = () => {
  return null;
};

export default Toast;